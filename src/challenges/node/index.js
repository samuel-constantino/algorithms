const { sumMultiples } = require('./sumMultiples');
const { sumPairsFibonacci } = require('./sumPairsFibonacci');
const { greatPrimeFactor } = require('./greatPrimeFactor');

module.exports = {
    sumMultiples,
    sumPairsFibonacci,
    greatPrimeFactor,
};
